// [<Export("my_gcd")>]
let myGcd (x: int) (y: int) =
  assert (y >= 0)

  if y = 0 then
    x
  else
    myGcd y (x % y)

// [<Export("my_multiple")>]
let myMultiple (x: int) (y: float) = float x * y
