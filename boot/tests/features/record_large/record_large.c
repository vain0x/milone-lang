#include "milone.h"

int defaultOptions_(int arg_);

int main();

int defaultOptions_(int arg_) {
    return 0;
}

int main() {
    struct GitCommitOptions_ call_ = defaultOptions_(0);
    struct GitCommitOptions_ _options_ = call_;
    return 0;
}
