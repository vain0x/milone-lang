// Test Std library.

open Std.ByteVectorTests
open Std.IO
open Std.FileTests
open Std.StdError
open Std.VectorTests

open Std.Block

let main _ =
  IO.exec (fun (io: IO) ->
    testByteVector ()
    testVector ()
    testFile io)
  |> never
