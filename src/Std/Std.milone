// Test Std library.

open Std.ByteVectorTests
open Std.IO
open Std.FileTests
open Std.StdError
open Std.VectorTests

let main _ =
  IO.exec (fun (io: IO) ->
    testByteVector ()
    testVector ()
    testFile io)
  |> never
